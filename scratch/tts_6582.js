"// reader/tts.js\n// 文字轉語音 (TTS) 控制引擎，負責語音列表加載、Edge WebSocket 流式合成、Web Audio API 無縫播放與高亮同步\n\n// 輔助函數：生成隨機的 ConnectionId (無連字號的 UUID)\nfunction connectId() {\n  return 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'.replace(/[x]/g, () => {\n    return (Math.random() * 16 | 0).toString(16);\n  });\n}\n\n// 輔助函數：取得 JavaScript 風格的 GMT 時間字串 (Microsoft Edge 協議要求)\nfunction dateToString() {\n  const months = [\"Jan\", \"Feb\", \"Mar\", \"Apr\", \"May\", \"Jun\", \"Jul\", \"Aug\", \"Sep\", \"Oct\", \"Nov\", \"Dec\"];\n  const days = [\"Sun\", \"Mon\", \"Tue\", \"Wed\", \"Thu\", \"Fri\", \"Sat\"];\n  const d = new Date();\n  const dayName = days[d.getUTCDay()];\n  const monthName = months[d.getUTCMonth()];\n  const date = d.getUTCDate().toString().padStart(2, '0');\n  const year = d.getUTCFullYear();\n  const hours = d.getUTCHours().toString().padStart(2, '0');\n  const minutes = d.getUTCMinutes().toString().padStart(2, '0');\n  const seconds = d.getUTCSeconds().toString().padStart(2, '0');\n  return `${dayName} ${monthName} ${date} ${year} ${hours}:${minutes}:${seconds} GMT+0000 (Coordinated Universal Time)`;\n}\n\n// 輔助函數：轉義 XML 字符，以符合 SSML 格式\nfunction escapeXml(unsafe) {\n  return unsafe.replace(/[<>&'\"]/g, (c) => {\n    switch (c) {\n      case '<': return '&lt;';\n      case '>': return '&gt;';\n      case '&': return '&amp;';\n      case '\\'': return '&apos;';\n      case '\"': return '&quot;';\n      default: return c;\n    }\n  });\n}\n\n// 輔助函數：將速率小數格式化為 prosody 的百分比字串 (例如 1.2x -> +20%, 0.8x -> -20%)\nfunction formatProsodyRate(rate) {\n  const percentage = Math.round((rate - 1.0) * 100);\n  if (percentage >= 0) {\n    return `+${percentage}%`;\n  } else {\n    return `${percentage}%`;\n  }\n}\n\n// 核心函數：在瀏覽器中動態計算 Sec-MS-GEC 簽名，防止 403 Forbidden 錯誤\nasync function generateSecMsG
<truncated 28451 bytes>