# -*- coding: utf-8 -*-
with open("/Users/burnfan/.gemini/antigravity/brain/d54f9316-a338-4dc0-a78e-3d268a8155cc/walkthrough.md", "a", encoding="utf-8") as f:
    f.write("\n\n---\n\n## 34. Fix Blank Pages in EPUB Books with Self-closing Script Tags\n\n### 根本原因 (Root Cause)\n許多現代 EPUB 圖書（特別是使用 Adobe InDesign、Kobo 等工具導出或加工的書籍）在 XHTML 內容檔（如 `ch1.xhtml`、`toc.xhtml` 等）的 `<head>` 區段中包含自定義的自閉合 `<script>` 標籤，例如：\n`<script xmlns=\"http://www.w3.org/1999/xhtml\" type=\"text/javascript\" src=\"../js/kobo.js\"/>`\n\n- 在標準的 HTML5 解析器（即 `DOMParser.parseFromString(..., 'text/html')`）中，`<script>` 是一個 Raw Text 元素，**不支援**自閉合寫法。HTML5 解析器會將 `/>` 視為普通的 `>` 開始標籤，並開始向後吞入後續的所有字元，將它們全部視為 `<script>` 的內容，直到遇到一個顯式的 `</script>` 閉合標籤為止。\n- 由於該 EPUB 中的所有 XHTML 檔案都沒有隨後對應的 `</script>`，導致**整個 XHTML 文件後續的所有內容（包括整個 <body> 標籤和全書的所有本文）都被當作該 script 標籤的內容全部吞掉**，導致 `body` 元素被解析為空。這直接造成了新書導入後「頁面完全空白、什麼內容都沒有」的嚴重 Bug。\n\n### 解決方案 (Solution)\n我們在 [epub-parser.js](file:///Users/burnfan/Documents/antigravity/mysterious-oppenheimer/reader/parsers/epub-parser.js) 的 `_parseTOC()` 和 `loadChapterContent()` 中實作了預處理防禦機制：\n1. **自閉合標籤的正則替換**：\n   在將 XHTML 內容傳送至 `DOMParser` 解析之前，使用正則表達式尋找所有自閉合寫法的 script 標籤：\n   `htmlText = htmlText.replace(/<script([^>]*?)\\/>/gi, '<script$1></script>');`\n   這能將 `<script .../>` 精準替換為標準、相容於 HTML5 解析器的 `<script ...></script>`。\n2. **TOC 與章節本文雙重預防**：\n   此處理同時應用於目錄解析與章節載入，確保目錄生成與正文 DOM 樹構建均能正確解析，文字和圖片完美渲染，無任何空白頁現象。\n")
print("Walkthrough updated successfully!")
