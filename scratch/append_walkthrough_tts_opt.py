# -*- coding: utf-8 -*-
with open("/Users/burnfan/.gemini/antigravity/brain/d54f9316-a338-4dc0-a78e-3d268a8155cc/walkthrough.md", "a", encoding="utf-8") as f:
    f.write("\n\n---\n\n## 36. Optimize TTS Chunk Size and Merge Paragraphs for Smoother Transitions\n\n### 根本原因 (Root Cause)\n在前一個版本的 TTS 語塊優化中，我們將句子按「父元素（ParentElement）」進行分組。這導致每個段落（`<p>`、`<li>`等）都會強制切分為一個獨立的語塊：\n- 雖然段落內朗讀非常連貫，但**每次切換段落（跨越 <p> 標籤）時，依然會重新創建 SpeechSynthesisUtterance 實例**，產生明顯的瀏覽器語音佇列切換停頓。\n- 此外，預設的 `MAX_CHUNK_LEN = 400` 字元使得每次發送給 TTS 語音引擎的文本長度偏短，未能充分發揮 Edge 雲端 Natural 語音在長文本下自然抑揚頓挫、連貫呼吸的優勢。\n\n### 解決方案 (Solution)\n我們優化了 [tts.js](file:///Users/burnfan/Documents/antigravity/mysterious-oppenheimer/reader/tts.js) 的 `_buildChunks()` 語塊生成邏輯：\n\n1. **跨段落合併語塊**：\n   - 移除了「跨越不同父元素（段落）即強制切分」的限制，允許普通段落、列表等元素在不超出長度上限的情況下進行物理合併。\n   - 段落之間的轉換現在直接包含在同一個 Utterance 內，交由 Edge 語音合成引擎**自然流暢地控制段落間的微小呼吸停頓**（約 0.3s 的自然停頓），徹底清除了段落切換時長達 1 秒的瀏覽器系統級延遲停頓。\n\n2. **提升語塊長度限制**：\n   - 將每個語塊的最大長度 `MAX_CHUNK_LEN` 從 400 字元顯著提升至 **1000 字元**。這使得每次傳送給 TTS 引擎的句子長度增加，語音語氣的連貫性、起承轉合更加流暢自然。\n\n3. **精確標題切分（Heading Shield）**：\n   - 雖然合併了普通段落，但我們實作了 `isHeadingTransition` 標題檢測邏輯：當前句或上一句的父元素是標題標籤（`<h1>` ~ `<h6>`）時，仍會強制進行語塊切分。這保證了**章節標題與正文之間依然保有合理、正式的朗讀停頓**，避免標題與正文被連在一起朗讀而產生突兀感。\n")
print("Walkthrough updated successfully with TTS optimization section!")
